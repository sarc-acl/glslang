#version 450

#extension GL_EXT_abort : require

layout(local_size_x = 1) in;

layout(set = 0, binding = 0) buffer Data {
    vec4 v;
} data;

// A 4-component float vector, %v4f.
void main() {
    abortEXT("vec4: %v4f", data.v);
}
